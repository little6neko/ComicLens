# 预先翻译提交等待优化实施计划

依据：`docs/superpowers/specs/2026-08-27-pretranslation-submit-latency-design.md`

## 阶段 1：调整创建成功回调

- 修改 `web/src/features/comic-detail/pretranslation-dialog.tsx`。
- 将创建成功回调改为同步回调，不再等待查询失效完成。
- 当响应包含批次时，使用返回的批次摘要替换当前漫画翻译概览缓存中的 `batch`，保留章节概览。
- 成功提示后立即关闭弹窗。
- 以不等待方式刷新当前漫画翻译概览和后台批次列表。
- 保留 `noWork` 与错误分支原有提示语义。

## 阶段 2：静态验证

- 运行前端格式化。
- 运行前端格式检查、ESLint 和生产构建。
- 运行 `git diff --check`。
- 检查目标文件 diff，确认已有弹窗圆角改动保持不变。

## 阶段 3：运行服务验收

- 复用当前监听 `0.0.0.0:8233` 的真实测试服务。
- 确认服务健康且前端构建产物更新。
- 保留测试服务运行，供用户实际点击检查提交反馈速度。
